"use client"

import { Layout, Section } from "@design/components"
import { useEffect } from "react"

const Portfolio = () => {
  useEffect(() => {}, [])

  return (
    <Layout>
      <Section>
        <div> portfolio</div>
      </Section>
    </Layout>
  )
}

export default Portfolio
