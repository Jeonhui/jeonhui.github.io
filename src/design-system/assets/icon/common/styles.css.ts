import {style} from "@vanilla-extract/css";

export const fillWidth = style({
    width: "100%"
})

export const fillHeight = style({
    height: "100%"
})

export const aspectRatioOne = style({
    aspectRatio: "1",
})